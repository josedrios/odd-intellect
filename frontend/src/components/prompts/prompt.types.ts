export type PromptApi = {
  id: number;
  text: string;
  created_at: Date;
  comment_count: number;
};

export type Prompt = {
  id: number;
  text: string;
  createdAt: Date;
  commentCount: number;
};
