export type Prompt = {
  // Need to make an api type and plug it to a "pretier" type
  // id: number;
  prompt_id: number;
  text: string;
  // createdAt: Date;
  start_date: Date;
  // commentCount: number;
};
