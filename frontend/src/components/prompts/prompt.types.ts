export type PromptApi = {
  id: number;
  text: string;
  created_at: string;
  comment_count: number;
};

export type Prompt = {
  id: number;
  text: string;
  createdAt: string;
  commentCount: number;
};
